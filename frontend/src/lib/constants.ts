export const QUERY_KEYS = {
  CURRENT_USER: "currentUser",
  USERS: "users",
} as const

export const PAGE_SIZE_OPTIONS = [5, 10, 25, 50] as const
export const DEFAULT_PAGE_SIZE = 10
export const DEFAULT_USERS_FETCH_LIMIT = 100
